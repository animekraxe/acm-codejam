#!/usr/bin/env python

import sys

while True:
    n = int(sys.stdin.readline().strip())
    if n == 0:
        break
    print int(
        sum(int(sys.stdin.readline().strip())
            for i in range(n))
        / n)
